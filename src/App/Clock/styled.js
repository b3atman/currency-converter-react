import styled from "styled-components";

export const Wrapper = styled.div`
    text-align: right;
    justify-self: flex-end;
    font-family: "Roboto", monospace;
    color: ${({ theme }) => theme.color.smalt};
`;